extends AudioStreamPlayer

func _ready():
	pass
	#var savefile = FileAccess.open('user://settings.dat', FileAccess.READ)
	#var _settings = savefile.get_var()
	#var volumedb = int(20*log(_settings.music_volume/100))
	#$audioplayer.volume_db = volumedb
